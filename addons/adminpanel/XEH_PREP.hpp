// GHOST'S HALF. The console's own screen, the seams it needs, and the gate.
// Everything else in this addon is the ported panel under its own admp_ tag -
// see CfgFunctions.hpp - which is also the half that is GPL.
PREP(adminList);
PREP(isAdmin);
PREP(open);
PREP(panelAdmin);

// The look, read off the tacpad's settings at display-open time
PREP(style);
PREP(styleMessage);

// The console's own controls, which the panel did not have
PREP(filterList);
PREP(mapFilter);
PREP(mapTabs);
PREP(mapTargets);
PREP(mapCursor);
PREP(execTarget);
PREP(execTabs);
PREP(execRun);
PREP(execClear);
PREP(execLog);
PREP(confirmEnd);
PREP(infoRow);
