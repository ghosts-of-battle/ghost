# Agent handoff — 2026-08-14

You are picking up the ghost mod (`o:\GIT\ghost`) and the Roomba mission
(`o:\OneDrive\Documents\Arma 3 - Other Profiles\YonV\missions\2040\
Task_Force_Roomba_dev.Tanoa`) at build **0.1.0.691**. Memory is current and
loads with the session — trust it, this file adds the session-local context
memory doesn't carry.

## Rules that are enforced by the user, not suggestions

- **Never** run `hemtt release`/`build` — the user builds; the game loads
  `.hemttout\release`. You run `hemtt check`, `tools/sqf_validator.py`,
  `tools/check_plumbing.py`, `tools/check_invariants.py`. All four are
  green at 691.
- **Never** edit other repos — `o:\GIT\ALiVE.OS`, cTab, BCE, ITC are
  read-only reference. Read their source before using any of their
  symbols; the user has zero tolerance for guessed ALiVE symbols.
- UI work is reported **"code-fixed — needs your eyes"**, never "done".
  No design-parity claims without the user's screenshot. They have called
  out false "done" claims repeatedly and will again.
- Every reply that touched the mod ends with the version from
  `addons/main/script_version.hpp`, bold, last line. BUILD increments per
  hemtt run, including your checks.
- No `.py` under `addons/`. No self-invented `#define` helpers. No
  single-line config class bodies. Parents restated on config patches
  (`class X {}` severs inheritance). Faction work is **frozen**.
- The user writes terse, typo-heavy messages, annotates screenshots with
  red boxes/arrows, sends corrections mid-turn, and says "fix" meaning
  *look at the image*. When they write "questions ?" they expect you to
  ask before building. Answer plainly when they ask "do you know what
  you're doing" — state the deliverable in one sentence.

## Untested boundary

**Nothing after build 689 has been run in-game.** The user tests in
irregular rounds and reports back with screenshots. Everything below is
validator-clean and unproven. When they report, expect several mid-turn
messages stacking corrections — handle all of them.

## Workstreams: state → next action

**TAC//SUPPORT** (`tacpad_apps/fnc_appSupport.sqf` + `adapter_alive`).
All three ALiVE legs wired (transport/CAS/**arty**). Screen is a dialog
(idd 8960) built as three slabs AROUND the map control — never create a
control over the map rect: runtime controls stack above config ones, that
was the dark-unclickable-map bug. Ground forced opaque in dialogs. Panel
rect literals must match `tacpad_apps/dialog.hpp`. Arty protocol facts are
in memory `project-tac-support` — the ten-element NewTask + status flip +
GoToBase are verbatim from ALiVE source, do not "improve" them. Task point
REQUIRED for arty. Next: user test round; then the handoff remainders
(transport PASSENGERS manifest, add-task screen, RTB double-confirm) if
asked.

**Close bug** — root cause and fix are in memory `close-fires-on-down`.
If any window "will not close" again: the answer is never another net
UNDER the close button; check what eats the mouse-DOWN from above, and
check whether a refresh loop redraws without a data-change gate.

**Leaders** (`ghost_leaders`). Non-player deaths now route to
`fnc_replace` (successor, corpse loses phone, empty car deleted); only
player kills / prison delivery reach `fnc_down`. The Killed EH checks
killer, instigator, and `effectiveCommander`.

**Signs** (`tools/gen_signs.py` → `addons/media/images/signs_v2[_plain]`).
- OPEN TASK: the user will re-supply the six-band MultiCam family sheet
  (Black/Woodland/Tropic/original/Arid/Alpine). When it arrives: cut
  Woodland→`mcw`, Tropic→`mypt`, Arid→`mtpa` at ~210 px source width into
  `addons/media/images/<name>_swatch.png`, switch their `SIGNS` entries to
  `U(..., "X_swatch.png", None, local=True)` (mtp/ocp already work this
  way), render, VERIFY BY READING THE PNG, convert PAA.
- If the image arrives as a chat paste with no file: it may still be in
  the Windows clipboard —
  `[System.Windows.Forms.Clipboard]::GetImage()` via PowerShell saved it
  once this session. The clipboard holds only the NEWEST paste.
- Render single signs with a small scratch script that imports
  `gen_signs` and calls `render()` per VARIANTS (scratchpad is
  session-scoped — yours is empty, rewrite it; ~20 lines, see the recipe
  encoded in `gen_signs.py` main()). **Never run a full `gen_signs.py`
  regen without `GHOST_SIGN_ORIG`** pointing at old-sign PNGs — the
  keybinds + remaining uniform boards lose their figures. Originals:
  `old/1/media/images/ghost/*.paa` (convert via `hemtt utils paa convert`)
  and `paa/mcw.png`.
- Verify sign output visually every time — Read the PNG. Two crop
  mistakes were only caught by looking.

**Mission MOTORPOOL** (mission, not mod — mission style uses TABS and its
own YMF conventions). Full map in memory `mission-motorpool`. Squad
configs (`config/<squad>/config_motorpool_<squad>.hpp`) are seeded EMPTY —
expect the user to ask for fills or do their own. Eden line:
`[this, padName] call YMF_fnc_motorpool_init;`. The cosmetic catalogue is
`fn_vehicle_cosmeticEntries` (pure data; code blocks read `_vehicle` from
the CALLER's scope — bind before `call`). Don't validate the mission with
ghost's sqf_validator directly — it flags the mission's tab indentation
wholesale; use the junction trick (scratch dir with `addons` junction →
mission `scripts`) and filter output to your files.

## Evidence sources (use before theorising)

- RPT: `C:\Users\jwise\AppData\Local\Arma 3\*.rpt` — newest file, grep for
  `Error in expression` and `ghost_`. The user's modline and loaded pbo
  versions are in the header — CHECK THE LOADED BUILD before concluding a
  fix "didn't work".
- Steam screenshots (what the user's annotated pastes are cropped from):
  `C:\Program Files (x86)\Steam\userdata\39736977\760\remote\107410\
  screenshots\`.
- Unpacked base game for config/p3d verification:
  `O:\OneDrive\Documents\Arma 3 Projects\a3`.
- ALiVE source: `o:\GIT\ALiVE.OS\addons\sup_combatsupport\scripts\
  NEO_radio\` (read-only).

## Open questions parked with the user

1. Six-band MultiCam sheet (they know; will re-send).
2. Tacpad palette — light Modernist vs current scheme setting — asked
   twice, never answered. Don't redesign around it; ask only if palette
   work comes up again.
3. `time > _t` RPT spam = vanilla Ambient Civilian Presence brain called
   by some third-party civ mod (NOT cTab, NOT ghost, NOT the mission) —
   waiting on the user to disable-test or paste the modset.

*Version on disk when this was written:* **0.1.0.691**
