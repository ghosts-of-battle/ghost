# Agent handoff — 2026-08-16

You are picking up the ghost mod (`o:\GIT\ghost`) and the Roomba mission
(`o:\OneDrive\Documents\Arma 3 - Other Profiles\YonV\missions\2040\
Task_Force_Roomba_dev.Tanoa`) at build **0.1.0.722**. Memory loads with the
session — trust it; this file carries the session-local context memory does
not, and supersedes `handoff-2026-08-14.md` where they disagree.

## Rules the user enforces (not suggestions)

Everything in the 08-14 handoff still stands. Reprinted because they were
tested again this session, plus what was added:

- **Never** run `hemtt release`/`build`. You run `hemtt check` and
  `tools/check_plumbing.py`. Both are green at 722.
- **Never** edit other repos. `o:\GIT\ALiVE.OS`, cTab, BCE, ITC, DDT, ELKA
  are read-only reference.
- UI work is reported **"code-fixed — needs your eyes"**, never "done".
- Every reply that touched the mod ends with the version from
  `addons/main/script_version.hpp`, bold, last line.
- **No `.py` generators and no scratch python edits.** Bulk config edits go
  through the Edit tool one file at a time, however tedious — 35 role files
  were edited that way this session.
- **NEW: the authority for ALiVE symbols is the WORKSHOP PBO, not
  `o:\GIT\ALiVE.OS`.** The checkout is a newer dev fork that has drifted
  (arty status pre-flip, last-shot marker, lbData ordnance). Unpack with
  `hemtt utils pbo unpack "…\!Workshop\@ALiVE\addons\sup_combatsupport.pbo"`
  into the scratchpad and read that. `grep -a` on a packed pbo lies — some
  strings hit, others silently miss.
- **NEW: panels do not drag.** Dragging never worked; every reference was
  purged at 719. Never tell the user to drag a panel or promise saved
  positions — panels are their `DEFAULT_*` defines, always.
- **NEW: purged means purged, and "add X back" needs a scope check.** The
  user asked for the old kamikaze drones back, then narrowed to "just the
  drones, not the old functions", then reversed that too. When a request
  could mean content or behaviour, ask before building.

## Untested boundary

**Almost nothing in this session has been proven in-game.** The user tests
in irregular rounds and reports with annotated screenshots. What IS proven:

- TAC//SUPPORT tasking reaches ALiVE (a fire mission fired — the user's
  next complaint was that only one gun of three answered, which is
  behaviour, not delivery).
- The reader, motorpool and HUD render (screenshots).

Everything else — the whole audit, the nets, BFT member icons, SITREP,
GUNS, FORCE CLOSE, the reply box, the motorpool paint fix, the whole
app-layer stuck-window pass (see workstream below) — is validator-clean and
unproven. Same for the ambience `droneSide` removal (D59): the derived-side
airframe selection has no commanders branch in game yet.

## THE OPEN INVESTIGATION: "it's raining drones"

This is the one live unknown. Read this before touching `ghost_uas`.

**Symptom:** drones fall out of the sky continuously on the dedicated
server; the user reports they arrive with no AI aboard.

**What the evidence actually shows** (server RPT
`arma3server_08_15_2026_220508.rpt`, build 716):

- 47 patrol drones spawned, crewed, and flew. **Zero** `spawns with no
  crew` warnings, **zero** killed, **zero** deleted.
- Yet `patrol(s) up` reads **0 for every side on every 60-second tick** —
  in that log, in every earlier server log, and in **every hosted-SP log on
  record**. The ledger empties with nothing logging a death.
- So: drones are not dying of anything that reports. Either the ledger
  loses them (the prune is its only remover) or they leave the world in a
  way nothing observed.

**Ruled out with source reads, do not re-litigate:**

- Drongos Drone Tweaks jammers — its `DDT_fnc_JamDrone` does delete crew,
  but only runs off its jammer backpack (`DDT_DroneJammerPack`) or placed
  heavy jammer objects. The user has neither. DDT's drone *register* is
  benign (mutes voices, disables combat reflexes).
- ELKA AntiUAV — a pure weapon mod, no scripts at all.
- Crew-class failure — the spawn path retries `createVehicleCrew`,
  hand-seats the drone's own declared crew class, then the side's vanilla
  `*_UAV_AI`, and **deletes the airframe with a named warning** if all
  three fail. No such warning has ever appeared.

**Instrumentation waiting for a server run on ≥717:**

- `patrol drone up: <class> for <side> at <grid>` — every spawn.
- `patrol drone killed at <grid> by '<class>' (<name>)` — names the killer.
- `patrol drone DELETED (not killed)` — a script or GC removed it.
- `prune drops <x> - isNull <b>, alive <b>` — why the ledger dropped an
  entry (fnc_livePatrols).
- `'<class>' cannot self-fly - it stalled onto the deck` — the 45-second
  stall test, which also deletes the husk.

**Next action:** get a server RPT from build ≥717, grep `patrol drone`,
`prune drops`, `stalled`. One of those five lines will name the mechanism.
Do not theorise further without it — four theories have already died.

**Also found while investigating:** the drone module's per-side class
fields were stocked with FPV/loitering **munitions** (`B_SwitchBlade_600`,
`I_Crocus_AP/AT`, `I_KVN_AP`). Those have no autonomous pilot and tumble
whatever crew they carry. Patrol fields want AI-flyable recon airframes;
munitions belong to the ambience kamikaze module's field. The stall test
now names any that slip in.

Mitigation already shipped (715): losses regenerate **one per side per
minute** (`UAS_REGEN_PER_TICK`, keyed on a seeded flag, not the live
count). Even at worst the sky cannot rain.

## Workstreams: state → next action

**TAC//SUPPORT** (`tacpad_apps/fnc_appSupport.sqf` + `adapter_alive`).
Tasking works. Six rounds of fixes are in memory `project-tac-support`;
the load-bearing ones:

- The **supportTag gate** blocked everyone for a day (default "JFO", a flag
  the mission never sets). Default is now empty and the gate fails open when
  the tag names nobody in the mission.
- **Control-level events never fire in this dialog.** The 703 press log
  proved input reaches the display while no control handler ever ran.
  CONFIRM / RTB / CLOSE / SITREP are dispatched from the display's own
  `MouseButtonDown`, hit-testing rects registered in
  `uiNamespace QGVAR(supportHotspots)`. If you add a button here, register a
  rect — do not attach a handler and hope.
- GUNS stepper writes ALiVE's own `supportWeaponCount`; ALiVE splits rounds
  round-robin across that many guns, reads it live at fire time, and a
  1-round mission always fires from the lead gun alone.
- SITREP (`adapter_alive/fnc_supportSitrep`) is airframes-only — an arty
  entry's element 0 is the battery leader, a man.

Next: user test of SITREP + GUNS; the handoff remainders (transport
PASSENGERS manifest, add-task screen, RTB double-confirm) if asked.

**TAC//MSG** (`messaging` + `tacpad` reader). Three items landed at 719:

- **Role net access.** `EFUNC(messaging,roleNets)` reads the mission's own
  role config (`nets[] = {{"HQ","true"}, …}` inside a `Dynamic_Roles`
  class), keyed off the mission's `YMF_role` unit variable. No configured
  value = no access; the gate is off entirely until one role declares
  `nets[]`. Personal box and own-squad net are never gated.
- **The reply box never worked** because it was a config-declared control
  and the reader's runtime screen group covers the whole dialog — the same
  runtime-above-config trap as the support map. It is `ctrlCreate`d last
  now by `FUNC(readerInput)`; **the reader dialog declares no controls at
  all**, deliberately.
- **ON NET** lists every player grouped by squad, own squad first, each head
  foldable (`uiNamespace QGVAR(readerNetFold)`), default expanded.

Mission side: nets are defined in `config/config_nets.hpp` (`class
Ghost_Nets`), which the mod prefers over its own setting. All 35 role files
carry `nets[]`: TL and ATL get C2 + FIRES.cas/.arty/.trans +
MEDICAL.medevac + all five squad nets (TL also ENGINEER); JFO gets all
FIRES; medics all MEDICAL; demo gets ENGINEER; ISR/recon squad-only.

**Tacpad app layer — windows stop getting stuck** (`tacpad` + `tacpad_apps`,
build 722). Prevention, not just a panic button, for the "window will not
close" family. The shell now closes four races itself:

- **Stale-reopen guard.** `openApp` refuses any reopen whose app is no longer
  the current one unless a panel/tile (the only humans) asks with `_fresh=true`
  (`fnc_panelTiles`, `fnc_panelSquad`, `fnc_panelSettings` pass it). Kills "I
  closed it and it opened again" — a reopen queued for next frame landing
  after the close.
- **Throw shield.** The app handler runs in `try/catch`; a draw that throws
  (logged `ERROR`) still hides the panels and draws the escape hatch instead
  of aborting `openApp` with `appCurrent` set to nothing visible.
- **Floating FORCE CLOSE strip** (`fnc_appBar`, NEW). Drawn over every open
  app, created after the app's group so nothing covers it, recreated on every
  redraw, removed by every close path plus a tick sweep in `fnc_open`. It
  also draws for apps that drew nothing (a SUPPORT gate refusing tasking
  before its dialog opens) so a blank screen is never a dead end.
- **Map-close cleanup.** `tacpad_apps` Map handler now clears the shell's
  `appBar` ref beside `appGroup`/`appCurrent`.

Every reopen call site was audited (13 apps, PFH + `execNextFrame` paths);
the guard is the single choke point, so a new app's redraws are covered
without touching it. Validator-clean, unproven in game — see the untested
boundary.

**BFT.** Auto-track is forced to **All** in the mission's `cba_settings.sqf`
(it was pinned to "Player", which hid every AI group). Members now draw as
**live vanilla unit icons** on the map's Draw event
(`fnc_drawMembers`, `iconman_ca.paa` rotated to heading, fire-team colours,
name) rather than markers — the group marker stays the call-sign anchor.
Setting: *Member markers* Off / Own squad / All tracked.

**ghost_ambience** (NEW addon, 709). Two Eden modules: Ambient Shelling and
Ambient Kamikaze Drones. Both pick a building near a player inside named
markers, both post to the alert bus first, both run through `taorGate`. The
kamikaze drone is killable and a dead drone detonates nothing. The old
`ambient_kamikaze` / `kamikaze` addons stay purged; this is a new identity.
Build 722: the kamikaze module's **Drone Side** combo is gone (D59 — sides
are typed once). Whose drones is never asked; the side is derived from the
adapter's commanders hostile to the players, exactly like `reaction/fnc_major`
reads the answering side, and the default east keeps the vanilla OPFOR
quadcopter when nobody is answering. This clears the last failing invariant.

**taorGate** (`common/fnc_taorGate`, NEW). The one pre-spawn side/TAOR check.
Wired into uas patrols (overwatch mode only), qrf squads (**hard** now — no
origin inside own ground means the QRF stays home), qrf asym mortars,
airdefence batteries and MANPAD teams, leaders, and reaction's fallback
barrage. Deliberately not wired into antiship (own markers), jamming's
placement API, or player-carried things. No TAOR defined = no gate.

**LAMBS off for support aircraft** (`common/fnc_lambsOff`, NEW). ALiVE tags
every combat-support asset with `ALIVE_CombatSupport`; the adapter sweeps on
that tag every POOL_TICK, so respawned airframes are caught too. Also
applied to uas patrols and kamikaze runs.

**Mission MOTORPOOL.** Init line is now
`[this, pad, cars, armor, mech, heli, plane, boat, static, heading, extras]`
— seven category switches (omitted = true), a spawn heading in degrees
(-1 = the pad's own facing), and an array of extra classnames this pad
offers on top of the configs (sorted onto tabs by kind, and beating the
switches). MECHANIZED is a new category. The paint list had **no click
handler at all** — fixed with `fn_motorpool_paintPick`, which marks the
chosen row (`>`), since the dark theme hides the engine's selection bar.
Tab row holds seven categories + SPAWNED = exactly full.

**main_menu JOIN.** The tile stalled on the server browser: upstream's
chain advances one frame per step and the browser now takes several. Each
stage waits for its own display, 20 s timeout, distinct message per stage.
A `connectToServer` one-liner rewrite was built and **reverted on the
user's instruction** — keep the script.

## The five bug families (full-mod audit, build 711)

Three audit agents plus a manual pass fixed ~25 verified bugs. Grep for
these first in any future work — every one appeared more than once:

1. **Stale callers after a signature refactor.** `fireBarrage` was
   refactored to `[pos, rounds, spread, shell, window]` and **three**
   callers still passed the old ammo-first shape (reaction, qrf, leaders),
   silently rescued by defaults. When you change a shared function's
   signature, grep every caller in the same turn.
2. **`exitWith` inside a `then{}`** exits only that block. Cost: reaction's
   SATCOM exemption never exempted anyone; hacking tower failures printed
   "Intrusion FAILED" and then delivered the hack anyway.
3. **Server-set values read on clients**, invisible in hosted SP because
   host == server. Cost: the whole radio-detection leg dead in MP, the
   hacking product menu empty for every remote player, pad rearm/refuel
   silently failing. Fixes: `publicVariable`, published client-safe
   summaries (`adapter_alive` `commandersPub`/`taorPub`), or per-unit
   variable mirrors (`reaction` `flaggedUntil`).
4. **Never-written GVARs killing whole addons.** `ghost_jamming` was
   entirely dead — nine model knobs read everywhere, defined nowhere.
   `equipment` and `boarding` were dead behind an `EGVAR(main,ghost_enabled)`
   gate nothing ever set.
5. **Locality.** Argument-local commands on remote objects
   (`setVehicleAmmo`, `setFuel`, `fullHealLocal`, ACM resets) need
   `remoteExec` at the owner.

Plus a recurring HEMTT trap: **`INFO_x`/`WARNING_x` macro arguments cannot
contain commas** — `[a,b]`, an inline `getVariable [x, y]`, or a `select`
over an array literal all read as extra macro arguments. Hoist to a local
first. Bare `break` is not an SQF command either.

## QUEUED: map-screen UI work (from the 2026-08-16 annotated screenshot)

Five items, none started, **no code written for any of them**. The user
marked up a map screenshot with three coloured boxes and two notes. Read
this whole section before touching a panel — items 1-3 are one idea, and
item 4 collides with a change the screenshot predates.

**The screenshot is from a build before 719.** Its strip row shows three
buttons (SETTINGS / ADMIN / HIDE ALL); FORCE CLOSE now sits between ADMIN
and HIDE. Any geometry you take off that image is one button out of date.

### 1-3. "Size to fit the red / blue / green box"

Three boxes drawn round three panels, all saying the same thing: **the
content does not fill its frame.**

- **Red — the live-tile band** (`tacpad_apps/fnc_panelTiles`,
  `DEFAULT_TILES [0.008, 0.031, 0.60, 0.11]`). Six tiles are drawn, the row
  ends short of the frame's right edge, and a `+1` sits in the gap - a
  seventh tile that had no room. Tiles should size to the band so all of
  them fit edge to edge. `TILE_W` in `tacpad/shared.inc.hpp` is a fixed
  fraction; the fix is to derive tile width from the frame width and the
  tile count rather than shipping a constant.
- **Blue — the squad rail** (`tacpad_apps/fnc_panelSquad`,
  `DEFAULT_SQUAD [0.008, 0.20, 0.150, 0.60]`). Two men listed at the top,
  the legend and REFRESH pinned to the foot, and a tall dead gap between
  them. The rail deliberately keeps its placed height (the comment at the
  `_bodyH` block explains why: sized to the section it was two inches tall
  for a fire team). So the frame stays and the CONTENT has to fill it.
- **Green — the reader** (`tacpad_apps/fnc_panelReader`,
  `DEFAULT_READER [0.72, 0.031, 0.27, 0.66]`). Three threads listed, then a
  large empty area above OPEN FULL / NEW. Same shape of problem: the list
  should use the height it has.

Note the tension with `EFUNC(tacpad,fit)`, which does the OPPOSITE - it
shrinks a panel to the rows it drew, and exists because an empty reader
used to be "a wall of white over the map". Neither the squad rail nor the
reader calls `fit` today (only tiles and tools do). **Decide which way each
panel goes and say so in the code**: either the frame shrinks to the
content (`fit`) or the content spreads to the frame. The user's boxes are
drawn around the FULL frames, and their note says "size to fit the box",
which reads as: keep the frame, fill it. Recommend confirming with one
screenshot before doing all three.

### 4. TOOLS button in the strip row, before SETTINGS

Toggles the MAP TOOLS panel. The panel is `tacpad_apps/fnc_panelTools`
(`DEFAULT_TOOLS [0.40, 0.845, 0.30, 0.11]`) and its visibility already has
a per-player setting, `ghost_tacpad_apps_show_tools`, which the shell reads
in `tacpad/fnc_open` as `format ["ghost_tacpad_apps_show_%1", _x]`. So the
button is a chromeless strip like the others whose press flips that
variable and re-opens the suite - the same shape as `fnc_panelHide`.

**The row is the problem, not the button.** After 719 it already holds four
strips across 0.72-0.99 at 0.0645 wide each (SETTINGS / ADMIN / FORCE CLOSE
/ HIDE). A fifth means ~0.051 each, and "FORCE CLOSE" is already tight in
0.0645. Options, in the order I would try them: shorten the labels (TOOLS,
SET, ADMIN, CLOSE, HIDE); or split into two rows of three; or drop the row
to icons. This is a design call - **ask the user, do not just squeeze**.
Note the MAP TOOLS panel also carries its own OFF button in its header, so
the new button and that one must agree about state.

### 5. HIDE ALL / SHOW ALL → HIDE / SHOW

One-line label change in `tacpad_apps/fnc_panelHide` (the
`["HIDE ALL", "SHOW ALL"] select _hidden` line). Nothing else reads those
strings. Do it in the same pass as item 4, since both touch that row and a
shorter word buys the space item 4 needs.

## Evidence sources (use before theorising)

- Client RPT: `C:\Users\jwise\AppData\Local\Arma 3\*.rpt` — newest file.
  **Check the loaded pbo version in the header before concluding a fix
  didn't work.**
- **Server RPT: the user downloads it to `C:\Users\jwise\Downloads\
  arma3server_*.rpt`.** The dedicated box runs its own copy of the mod —
  a client on the new build against a server on the old one proves nothing.
- Unpacked workshop ALiVE (this session's copy):
  scratchpad `alive_ws\sup_combatsupport`. Re-unpack if the scratchpad is
  gone; it is session-scoped.
- Unpacked base game: `O:\OneDrive\Documents\Arma 3 Projects\a3`.
- Mod sources worth reading rather than guessing about: `@Drongos Drone
  Tweaks` (drone AI/jammers), `@ELKA AntiUAV`, `o:\GIT\CBA_A3`,
  `o:\GIT\ACE3`, `o:\GIT\task-force-arma-3-radio`.

## Open items parked

1. **Raining drones** — awaiting a server RPT from build ≥717. Top priority.
1b. **The five map-screen UI items** in the queued section above — not
   started, and two of them need a decision from the user before code.
2. **`ghost_modules` Safe Start module is unfinished** — its trigger has no
   statements and nothing reads it. It now WARNS in the RPT instead of
   silently doing nothing. Ask the user what it should do before building.
3. The audit's WATCH list (unverified suspicions, deliberately not churned)
   is in memory `audit-2026-08-15`.
4. Tacpad palette question (light Modernist vs scheme setting) — asked
   three times across sessions, never answered. Do not redesign around it.
5. `docs/OVERVIEW.md` (hand-written, not generated) is the plain-language
   "what does this mod add" page if the user asks again.

*Version on disk when this was written:* **0.1.0.722**
