class CfgFactionClasses {
    class ghost_I_Taliban {
        displayName = "[Ghost] Taliban";
        priority = 3;
        side = 2; // Independent / GUER
        icon = QPATHTOF(data\icon_taliban_ca.paa);
        flag = QPATHTOF(data\flag_taliban_co.paa);
    };
};
